# Produção — API no servidor definitivo (VPS · app.airtonartus.com.br).
# Usado no build web e nos apps Capacitor. No build feito NO próprio VPS,
# sobrescrevemos com VITE_API_URL= (vazio) para usar /api same-origin.
VITE_API_URL=https://app.airtonartus.com.br/api

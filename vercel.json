{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install --prefix frontend && npm install --prefix backend && npm --prefix backend run prisma:generate",
  "buildCommand": "npm --prefix frontend run build",
  "outputDirectory": "frontend/dist",
  "redirects": [
    { "source": "/(.*)", "destination": "https://app.airtonartus.com.br/$1", "permanent": true }
  ]
}
